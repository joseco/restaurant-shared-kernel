name: publish to nuget
on:
  push:
    branches:
      - main # Default release branch, may also be named 'master' or 'develop'
jobs:
  publish:
    name: build, pack & publish
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Setup .NET
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: 6.0.x

      - name: Adding github registry
        run: dotnet nuget add source --username ${{ secrets.GH_USERNAME }} --password ${{ secrets.NUGET_API_KEY }} --store-password-in-clear-text --name github "https://nuget.pkg.github.com/${{ secrets.REPO_OWNER }}/index.json"

      - name: Packing 
        run: dotnet pack SharedKernel/SharedKernel.csproj --configuration Release

      - name: Pushing nuget package 
        run: dotnet nuget push  SharedKernel/bin/Release/SharedKernel.1.0.0.nupkg --source "github"
